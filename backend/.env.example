# 複製為本機環境變數後再啟動後端。實際值不要提交。
# PowerShell:  $env:JWT_SECRET="..."
# bash:        export JWT_SECRET="..."

DB_HOST=localhost
DB_PORT=3306
DB_NAME=dynamic_survey
DB_USERNAME=root
DB_PASSWORD=

# HS512 需要至少 512 bits。產生方式：
#   openssl rand -base64 64
JWT_SECRET=
JWT_EXPIRATION=86400000
